import { SelfLearningDashboard } from '@/components/SelfLearning';
// No changes needed for SelfLearning as it was ported with light theme styles.
// Wait, I should double check if I missed anything.
// Looking at previous turn, `SelfLearning` used `bg-gray-50` and `text-gray-900`.
// So it should be fine.

export default function Page() {
    return <SelfLearningDashboard />;
}
